---
title: "Org mode basics"
tags: [emacs, org]
---

A demo note in the `emacs` collection, rendered with the theme's `emacs` layout.

Use this collection as a small wiki: each file is a note, listed on the
[Emacs]({{ '/emacs/' | relative_url }}) index page via the `emacs-list-item` include.
